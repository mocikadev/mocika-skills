# 私有技能库 (Private Skills Repository)

本仓库 (`mocika-skills`) 专门用于存放与日常工作高度相关的、包含一定私密性和业务逻辑的专属 AI 代理技能 (Skills)。此仓库将作为私有仓库托管在 GitHub 上，以确保业务信息的安全。

## 目录导航与仓库结构

本仓库采用扁平化管理，**每个顶层目录代表一个独立且完整的 Skill 包**。

```text
mocika-skills/
  ├── skill-name-1/          # 独立的技能 1
  │   ├── SKILL.md           # 技能主指令
  │   ├── assets/            # 静态资源 (图片等)
  │   ├── references/        # 参考文档/上下文
  │   └── scripts/           # 该技能专属脚本
  ├── skill-name-2/          # 独立的技能 2
  ├── README.md              # 仓库说明
  └── AGENTS.md              # 代理导航与全局约束 (当前文件)
```

## 核心约束与规范

1. **私密与安全隔离**: 
   - 严禁将本仓库设置为公开 (Public)。
   - **绝对禁止**在任何技能中硬编码真实的生产环境密码、数据库 Token 等高敏凭证（应当指导代理通过环境变量或配置中心获取）。
2. **规范化结构**: 
   - 每个技能**必须**是自包含的，拥有独立的 `SKILL.md`，并在其内部维护各自的 `scripts`、`references` 等目录。
   - 新技能的创建必须遵循标准规范，建议直接使用 `create-skill` 技能在根目录下生成标准模板。
3. **语言要求**:
   - 技能指令 (`SKILL.md`)、规范与注释应使用**简体中文**编写。
4. **根目录纯洁性**:
   - 根目录只允许存在 skill 目录、`README.md` 和 `AGENTS.md`，**严禁**在根目录下创建任何非 skill 的目录或文件。
   - **严禁**将外部代码仓库（无论是克隆、子模块还是手动拷贝）放入本仓库。调查、研究、参考外部源码时，应在本仓库**之外**的临时目录操作，不得产生任何遗留文件在本仓库中。

## 常用工作流

- **初始化新技能**:
  加载 `create-skill` 技能，直接在仓库根目录（`/`）下创建新的技能文件夹。
- **本地链接**:
  利用 `skm` (Skill Package Manager) 或 `opencode` 的本地搜索路径机制，将本仓库内的特定技能包（如 `skm link ./skill-name-1`）挂载到你的日常工作流中。
